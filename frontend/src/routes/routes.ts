const ROOT = "/";

// Auth Routes
const LOGIN = "/login";
const HOME = "/home";
const REGISTER = "/register";

const routes = {
  ROOT,
  LOGIN,
  HOME,
  REGISTER,
};

export default routes;
